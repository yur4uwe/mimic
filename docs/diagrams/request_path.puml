@startuml FUSE_Request_Path
title Шлях запиту читання файлу через FUSE до WebDAV Cloud
skinparam sequenceMessageAlign center
skinparam boxPadding 10

box "Простір ядра (Kernel Space)" #LightGray
    participant "VFS / System Call" as VFS
    participant "FUSE Kernel Module" as FUSE
end box

box "Простір користувача (User Space)" #White
    participant "App\n(Word, ls, etc.)" as App
    participant "FUSE mount Daemon" as Fmount
end box

participant "WebDAV Cloud" as Cloud

== Ініціація запиту ==
App -> VFS: Читання файлу (read)
VFS -> FUSE: Перенаправлення запиту
FUSE -> Fmount: FUSE Op (Read Request)

== Обробка у сервісі FUSE ==
activate Fmount
Fmount -> Fmount: Трансляція у HTTP
Fmount -> Cloud: HTTP GET (Range: bytes)
activate Cloud
Cloud --> Fmount: 206 Partial Content (Data)
deactivate Cloud
Fmount -> FUSE: Повернення даних
deactivate Fmount

== Повернення результату ==
FUSE -> VFS: Дані у буфер ядра
VFS -> App: Результат читання

@enduml